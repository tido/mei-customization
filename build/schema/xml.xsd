<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified" targetNamespace="http://www.w3.org/XML/1998/namespace" xmlns:mei="http://www.music-encoding.org/ns/mei">
  <xs:import namespace="http://www.music-encoding.org/ns/mei" schemaLocation="tido.xsd"/>
  <xs:attribute name="base" type="mei:tido_data.URI">
    <xs:annotation>
      <xs:documentation>Provides a base URI reference with which applications can resolve relative URI references into absolute URI references.</xs:documentation>
    </xs:annotation>
  </xs:attribute>
  <xs:attribute name="id" type="xs:ID">
    <xs:annotation>
      <xs:documentation>Regularizes the naming of an element and thus facilitates building links between it and other resources. Each id attribute within a document must have a unique value.</xs:documentation>
    </xs:annotation>
  </xs:attribute>
</xs:schema>
